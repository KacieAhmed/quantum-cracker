/**
 * The pinned first candidate on corpus/quantum runs (Kacie's calibration
 * anchor): every lottery tests it FIRST, labeled "pinned — not random", so
 * timing is reproducible. Mirrors CALIBRATION_PHRASE in api/src/server.ts.
 */
export const CALIBRATION_PHRASE =
  "permit bean gaze lawsuit expect exclude poet mercy enrich measure ocean since";
